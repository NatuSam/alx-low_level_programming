#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
char put[8] = ('_', 'p', 'u', 't', 'c', 'h','a', 'r');
for (n = 0; n < 8; n++)
{
_putchar(put[n]);
}
return (0);
}
