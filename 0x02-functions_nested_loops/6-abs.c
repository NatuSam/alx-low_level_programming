#include "main.h"
#include <stdlib.h>
/**
 * _abs - return abs of n
 * @n: variable to determine if input is lowercase
 * Return: Always x (Success)
 */
int _abs(int n)
{
int x;
x = abs(n);
return (x);
}
